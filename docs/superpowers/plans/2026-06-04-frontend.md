# Prompt Arena Frontend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Streamlit 기반 MVP 프론트엔드 구현 — 방 생성/입장 → 대기 → 프롬프트 작성 → 결과 확인

**Architecture:** 단일 `app.py`가 `st.session_state["screen"]`으로 화면 라우팅. WS 수신은 백그라운드 스레드 + `queue.Queue`. 각 화면은 `screens/` 하위 독립 모듈.

**Tech Stack:** Streamlit 1.35+, websocket-client, requests, streamlit-autorefresh, python-dotenv

---

## 개발 규칙

- 태스크 하나 완료 → 버그 수정 → 테스트 가이드 제공 → 사용자 승인 → 다음 태스크
- 백엔드(`backend/`)는 수정하지 않음
- 모든 파일은 `frontend/` 하위에 생성

---

## 파일 맵

| 파일 | 역할 |
|------|------|
| `frontend/app.py` | 진입점, screen 라우팅 |
| `frontend/state.py` | session_state 키 상수 + init_state() |
| `frontend/api_client.py` | REST 호출 (create_room, get_room) |
| `frontend/ws_client.py` | WS 연결, 백그라운드 수신 스레드 |
| `frontend/screens/__init__.py` | 빈 파일 |
| `frontend/screens/home.py` | 홈 화면 |
| `frontend/screens/waiting.py` | 대기 화면 |
| `frontend/screens/round.py` | 라운드 화면 |
| `frontend/screens/result.py` | 결과 화면 |
| `frontend/requirements.txt` | 의존성 |
| `frontend/.env` | BACKEND_URL, WS_URL |

---

## Task 1: 프로젝트 뼈대

**Files:**
- Create: `frontend/requirements.txt`
- Create: `frontend/.env`
- Create: `frontend/state.py`
- Create: `frontend/app.py`
- Create: `frontend/screens/__init__.py`
- Create: `frontend/screens/home.py` (stub)
- Create: `frontend/screens/waiting.py` (stub)
- Create: `frontend/screens/round.py` (stub)
- Create: `frontend/screens/result.py` (stub)

- [ ] **Step 1: requirements.txt 생성**

```
streamlit>=1.35.0
streamlit-autorefresh>=1.0.1
requests>=2.31.0
websocket-client>=1.8.0
python-dotenv>=1.0.0
```

- [ ] **Step 2: .env 생성**

```
BACKEND_URL=http://localhost:8000
WS_URL=ws://localhost:8000
```

- [ ] **Step 3: 가상환경 + 의존성 설치**

```powershell
cd frontend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

- [ ] **Step 4: state.py 생성**

```python
from __future__ import annotations
import queue
import uuid
import streamlit as st

# 네비게이션
SCREEN = "screen"

# 세션 식별
CLIENT_ID = "client_id"

# 방 정보
ROOM_CODE = "room_code"

# WebSocket
WS_THREAD = "ws_thread"
WS_QUEUE = "ws_queue"
WS_CONNECTED = "ws_connected"
WS_OBJECT = "ws_object"

# 라운드 데이터
TASK = "task"
MODEL = "model"
TIME_LIMIT = "time_limit"
ROUND_START_AT = "round_start_at"

# 결과
RESULT_PAYLOAD = "result_payload"

# 제출 상태
SUBMITTED = "submitted"


def init_state() -> None:
    defaults = {
        SCREEN: "home",
        CLIENT_ID: str(uuid.uuid4()),
        ROOM_CODE: "",
        WS_THREAD: None,
        WS_QUEUE: queue.Queue(),
        WS_CONNECTED: False,
        WS_OBJECT: None,
        TASK: "",
        MODEL: "",
        TIME_LIMIT: 180,
        ROUND_START_AT: 0.0,
        RESULT_PAYLOAD: None,
        SUBMITTED: False,
    }
    for key, val in defaults.items():
        if key not in st.session_state:
            st.session_state[key] = val


def reset_game_state() -> None:
    """홈으로 돌아갈 때 게임 관련 상태 초기화."""
    game_keys = [
        ROOM_CODE, WS_THREAD, WS_CONNECTED, WS_OBJECT,
        TASK, MODEL, ROUND_START_AT, RESULT_PAYLOAD, SUBMITTED,
    ]
    for key in game_keys:
        if key in st.session_state:
            del st.session_state[key]
    # WS_QUEUE는 새로 생성
    st.session_state[WS_QUEUE] = queue.Queue()
```

- [ ] **Step 5: 각 화면 stub 생성**

`frontend/screens/__init__.py`:
```python
```

`frontend/screens/home.py`:
```python
import streamlit as st


def render() -> None:
    st.title("Prompt Arena")
    st.write("홈 화면 (개발 중)")
```

`frontend/screens/waiting.py`:
```python
import streamlit as st


def render() -> None:
    st.title("대기 중")
    st.write("대기 화면 (개발 중)")
```

`frontend/screens/round.py`:
```python
import streamlit as st


def render() -> None:
    st.title("라운드")
    st.write("라운드 화면 (개발 중)")
```

`frontend/screens/result.py`:
```python
import streamlit as st


def render() -> None:
    st.title("결과")
    st.write("결과 화면 (개발 중)")
```

- [ ] **Step 6: app.py 생성**

```python
from __future__ import annotations

from dotenv import load_dotenv

load_dotenv()

import streamlit as st

import state as s
from screens import home, waiting, round as round_screen, result

st.set_page_config(page_title="Prompt Arena", page_icon="⚔️", layout="centered")

s.init_state()

screen = st.session_state[s.SCREEN]

if screen == "home":
    home.render()
elif screen == "waiting":
    waiting.render()
elif screen == "round":
    round_screen.render()
elif screen == "result":
    result.render()
else:
    st.error(f"알 수 없는 화면: {screen}")
```

- [ ] **Step 7: 실행 확인**

```powershell
cd frontend
streamlit run app.py
```

브라우저에서 `http://localhost:8501` 열기. "홈 화면 (개발 중)" 텍스트 보이면 성공.

- [ ] **Step 8: 커밋**

```bash
git add frontend/
git commit -m "feat: 프론트엔드 프로젝트 뼈대 생성"
```

---

## Task 2: api_client.py + 홈 화면

**Files:**
- Create: `frontend/api_client.py`
- Modify: `frontend/screens/home.py`

**전제:** 백엔드 서버 실행 중 (`python run.py` in `backend/`)

- [ ] **Step 1: api_client.py 생성**

```python
from __future__ import annotations

import os
import requests

BACKEND_URL = os.getenv("BACKEND_URL", "http://localhost:8000")


def create_room(client_id: str) -> str:
    """방을 생성하고 room_code를 반환. 실패 시 예외 발생."""
    resp = requests.post(
        f"{BACKEND_URL}/api/rooms",
        headers={"X-Client-ID": client_id},
        timeout=5,
    )
    resp.raise_for_status()
    return resp.json()["room_code"]


def get_room(room_code: str) -> dict:
    """방 상태 조회. 실패 시 예외 발생."""
    resp = requests.get(
        f"{BACKEND_URL}/api/rooms/{room_code}",
        timeout=5,
    )
    resp.raise_for_status()
    return resp.json()
```

- [ ] **Step 2: 홈 화면 구현**

`frontend/screens/home.py`:
```python
from __future__ import annotations

import streamlit as st

import api_client
import state as s


def render() -> None:
    st.title("⚔️ Prompt Arena")
    st.caption("프롬프트로 승부하라")
    st.divider()

    col1, col2 = st.columns(2)

    with col1:
        if st.button("🆕 새 방 만들기", use_container_width=True):
            _create_room()

    with col2:
        if st.button("🚪 방 참여하기", use_container_width=True):
            st.session_state["_show_join_input"] = True

    if st.session_state.get("_show_join_input"):
        code = st.text_input("방 코드 입력 (4자리)", max_chars=4, key="join_code_input")
        if st.button("입장", key="join_btn"):
            if len(code) == 4:
                _join_room(code)
            else:
                st.error("방 코드는 4자리 숫자입니다.")


def _create_room() -> None:
    client_id = st.session_state[s.CLIENT_ID]
    try:
        room_code = api_client.create_room(client_id)
        st.session_state[s.ROOM_CODE] = room_code
        st.session_state[s.SCREEN] = "waiting"
        st.rerun()
    except Exception as e:
        st.error(f"방 생성 실패: {e}")


def _join_room(room_code: str) -> None:
    try:
        room = api_client.get_room(room_code)
        if room["status"] not in ("WAITING",):
            st.error("입장할 수 없는 방입니다. (이미 시작되었거나 존재하지 않음)")
            return
        st.session_state[s.ROOM_CODE] = room_code
        st.session_state[s.SCREEN] = "waiting"
        st.rerun()
    except requests.exceptions.HTTPError as e:
        if e.response.status_code == 404:
            st.error("존재하지 않는 방 코드입니다.")
        else:
            st.error(f"오류: {e}")
    except Exception as e:
        st.error(f"서버에 연결할 수 없습니다: {e}")
```

> `import requests` 추가 필요. 파일 상단에 `import requests` 추가.

- [ ] **Step 3: home.py 상단 import 확인**

`home.py` 상단:
```python
from __future__ import annotations

import requests
import streamlit as st

import api_client
import state as s
```

- [ ] **Step 4: 실행 + 동작 확인**

백엔드 먼저 실행:
```powershell
# 터미널 1 (backend/)
cd backend
.\.venv\Scripts\Activate.ps1
python run.py
```

프론트엔드 실행:
```powershell
# 터미널 2 (frontend/)
cd frontend
.\.venv\Scripts\Activate.ps1
streamlit run app.py
```

- [ ] **Step 5: 커밋**

```bash
git add frontend/api_client.py frontend/screens/home.py
git commit -m "feat: api_client + 홈 화면 구현"
```

**[사용자 테스트 가이드 - Task 2]**
1. 백엔드/프론트엔드 모두 실행
2. `http://localhost:8501` 접속
3. "새 방 만들기" 클릭 → 대기 화면("대기 중 (개발 중)")으로 이동 확인
4. 홈으로 새로고침 후 "방 참여하기" → 잘못된 코드 입력 → 에러 메시지 확인
5. "방 참여하기" → 3번에서 생성된 방 코드 입력 → 대기 화면 이동 확인

---

## Task 3: ws_client.py

**Files:**
- Create: `frontend/ws_client.py`

- [ ] **Step 1: ws_client.py 생성**

```python
from __future__ import annotations

import json
import os
import queue
import threading

import websocket

WS_URL = os.getenv("WS_URL", "ws://localhost:8000")


def connect(room_code: str, client_id: str) -> websocket.WebSocket:
    """WebSocket 연결 수립. 실패 시 예외 발생."""
    url = f"{WS_URL}/ws/arena/{room_code}?client_id={client_id}"
    ws = websocket.WebSocket()
    ws.connect(url)
    return ws


def start_recv_thread(ws: websocket.WebSocket, q: queue.Queue) -> threading.Thread:
    """백그라운드에서 ws.recv() 루프 실행. 수신 메시지를 q에 적재."""
    def _loop() -> None:
        while True:
            try:
                raw = ws.recv()
                if raw:
                    q.put(json.loads(raw))
            except Exception:
                break

    t = threading.Thread(target=_loop, daemon=True)
    t.start()
    return t


def send_join(ws: websocket.WebSocket) -> None:
    ws.send(json.dumps({"action": "JOIN"}))


def send_submit(ws: websocket.WebSocket, prompt_text: str) -> None:
    ws.send(json.dumps({"action": "SUBMIT", "prompt_text": prompt_text}))


def close(ws: websocket.WebSocket) -> None:
    try:
        ws.close()
    except Exception:
        pass
```

- [ ] **Step 2: 커밋**

```bash
git add frontend/ws_client.py
git commit -m "feat: ws_client 백그라운드 수신 스레드 구현"
```

---

## Task 4: 대기 화면

**Files:**
- Modify: `frontend/screens/waiting.py`
- Modify: `frontend/screens/home.py` (WS 연결 추가)

- [ ] **Step 1: home.py에 WS 연결 추가**

`_create_room()`과 `_join_room()` 함수에서 `screen = "waiting"` 전에 WS 연결:

```python
# home.py 상단 import에 추가
import ws_client


def _connect_ws_and_go_waiting() -> None:
    """WS 연결 후 대기 화면으로 이동. 연결 실패 시 에러 표시."""
    client_id = st.session_state[s.CLIENT_ID]
    room_code = st.session_state[s.ROOM_CODE]
    q = st.session_state[s.WS_QUEUE]

    try:
        ws = ws_client.connect(room_code, client_id)
        t = ws_client.start_recv_thread(ws, q)
        ws_client.send_join(ws)
        st.session_state[s.WS_OBJECT] = ws
        st.session_state[s.WS_THREAD] = t
        st.session_state[s.WS_CONNECTED] = True
        st.session_state[s.SCREEN] = "waiting"
        st.rerun()
    except Exception as e:
        st.error(f"게임 서버 연결 실패: {e}")
```

`_create_room()` 수정:
```python
def _create_room() -> None:
    client_id = st.session_state[s.CLIENT_ID]
    try:
        room_code = api_client.create_room(client_id)
        st.session_state[s.ROOM_CODE] = room_code
        _connect_ws_and_go_waiting()
    except Exception as e:
        st.error(f"방 생성 실패: {e}")
```

`_join_room()` 수정:
```python
def _join_room(room_code: str) -> None:
    try:
        room = api_client.get_room(room_code)
        if room["status"] not in ("WAITING",):
            st.error("입장할 수 없는 방입니다.")
            return
        st.session_state[s.ROOM_CODE] = room_code
        _connect_ws_and_go_waiting()
    except requests.exceptions.HTTPError as e:
        if e.response.status_code == 404:
            st.error("존재하지 않는 방 코드입니다.")
        else:
            st.error(f"오류: {e}")
    except Exception as e:
        st.error(f"서버에 연결할 수 없습니다: {e}")
```

- [ ] **Step 2: waiting.py 구현**

```python
from __future__ import annotations

import queue as q_module

import streamlit as st
from streamlit_autorefresh import st_autorefresh

import state as s


def render() -> None:
    st_autorefresh(interval=500, key="waiting_refresh")

    room_code = st.session_state[s.ROOM_CODE]
    st.title("⏳ 대기 중")
    st.write("방 코드를 상대방에게 알려주세요.")
    st.code(room_code, language=None)
    st.info("상대방을 기다리는 중입니다...")

    _poll_events()


def _poll_events() -> None:
    ws_queue: q_module.Queue = st.session_state[s.WS_QUEUE]
    changed = False

    while True:
        try:
            event = ws_queue.get_nowait()
        except q_module.Empty:
            break

        ev = event.get("event")
        if ev == "ROUND_START":
            st.session_state[s.TASK] = event["task"]
            st.session_state[s.MODEL] = event["model"]
            st.session_state[s.TIME_LIMIT] = event["time_limit"]
            import time
            st.session_state[s.ROUND_START_AT] = time.time()
            st.session_state[s.SCREEN] = "round"
            changed = True
        elif ev == "WAITING":
            pass  # 이미 대기 화면, 무시
        elif ev == "ERROR":
            st.error(event.get("message", "오류가 발생했습니다."))
            st.session_state[s.SCREEN] = "home"
            s.reset_game_state()
            changed = True

    if changed:
        st.rerun()
```

- [ ] **Step 3: 커밋**

```bash
git add frontend/screens/home.py frontend/screens/waiting.py
git commit -m "feat: 대기 화면 + WS 연결 구현"
```

**[사용자 테스트 가이드 - Task 4]**
1. 백엔드/프론트엔드 실행
2. **탭 A**: "새 방 만들기" → 방 코드 확인 (예: 1234)
3. **탭 B**: 같은 방 코드로 "방 참여하기" → 입장
4. 두 탭 모두 "대기 중" 화면 표시 확인
5. 탭 B가 JOIN하면 두 탭 모두 "라운드 화면 (개발 중)"으로 전환 확인

---

## Task 5: 라운드 화면

**Files:**
- Modify: `frontend/screens/round.py`

- [ ] **Step 1: round.py 구현**

```python
from __future__ import annotations

import queue as q_module
import time

import streamlit as st
from streamlit_autorefresh import st_autorefresh

import state as s
import ws_client


def render() -> None:
    st_autorefresh(interval=1000, key="round_refresh")

    _poll_events()

    # 라운드 데이터
    task = st.session_state[s.TASK]
    model = st.session_state[s.MODEL]
    time_limit = st.session_state[s.TIME_LIMIT]
    start_at = st.session_state[s.ROUND_START_AT]
    submitted = st.session_state[s.SUBMITTED]

    # 남은 시간
    elapsed = time.time() - start_at
    remaining = max(0.0, time_limit - elapsed)
    minutes = int(remaining) // 60
    seconds = int(remaining) % 60

    st.title("✍️ 프롬프트 대전")
    st.info(f"**과제:** {task}")
    st.caption(f"모델: {model}")

    # 타이머
    if remaining > 0:
        st.metric("남은 시간", f"{minutes:02d}:{seconds:02d}")
    else:
        st.error("⏰ 시간 초과!")
        if not submitted:
            # 타임아웃 — 서버 이벤트 대기
            st.info("서버 처리 중...")
        return

    st.divider()

    if submitted:
        st.success("제출 완료! 상대방을 기다리는 중...")
        return

    # 프롬프트 입력
    prompt = st.text_area(
        "프롬프트를 작성하세요",
        height=200,
        max_chars=1200,
        key="prompt_input",
        placeholder="여기에 프롬프트를 작성하세요...",
    )
    char_count = len(prompt)
    color = "red" if char_count > 1100 else "normal"
    st.caption(f"글자수: {char_count} / 1200")

    if st.button("📤 제출", type="primary", use_container_width=True):
        if char_count == 0:
            st.warning("프롬프트를 입력해주세요.")
        else:
            ws = st.session_state[s.WS_OBJECT]
            ws_client.send_submit(ws, prompt)
            st.session_state[s.SUBMITTED] = True
            st.rerun()


def _poll_events() -> None:
    ws_queue: q_module.Queue = st.session_state[s.WS_QUEUE]
    changed = False

    while True:
        try:
            event = ws_queue.get_nowait()
        except q_module.Empty:
            break

        ev = event.get("event")
        if ev in ("RESULT", "TIMEOUT", "ERROR"):
            st.session_state[s.RESULT_PAYLOAD] = event
            st.session_state[s.SCREEN] = "result"
            changed = True
        elif ev == "WAITING":
            pass  # 제출 후 대기 중, 무시

    if changed:
        st.rerun()
```

- [ ] **Step 2: 커밋**

```bash
git add frontend/screens/round.py
git commit -m "feat: 라운드 화면 (타이머 + 글자수 + 제출) 구현"
```

**[사용자 테스트 가이드 - Task 5]**
1. 탭 A/B에서 방 생성 후 매칭
2. 라운드 화면 진입 확인 — 과제, 모델, 타이머 표시 확인
3. 타이머 카운트다운 동작 확인
4. 프롬프트 입력 → 글자수 실시간 갱신 확인
5. **탭 A**: 프롬프트 작성 후 "제출" → "제출 완료! 상대방 기다리는 중..." 표시 확인
6. **탭 B**: 동일하게 제출 → 두 탭 모두 "결과 화면 (개발 중)"으로 이동 확인
7. (선택) 아무것도 입력 않고 180초 대기 → 타임아웃 처리 확인

---

## Task 6: 결과 화면

**Files:**
- Modify: `frontend/screens/result.py`

- [ ] **Step 1: result.py 구현**

```python
from __future__ import annotations

import streamlit as st

import state as s


def render() -> None:
    payload = st.session_state.get(s.RESULT_PAYLOAD) or {}
    event_type = payload.get("event", "RESULT")

    _show_banner(event_type, payload)

    if event_type == "RESULT" and payload.get("my_data"):
        st.divider()
        _show_comparison(payload)

    st.divider()
    if st.button("🔄 다시 하기", use_container_width=True):
        _go_home()


def _show_banner(event_type: str, payload: dict) -> None:
    if event_type == "RESULT":
        result = payload.get("result", "")
        if result == "WIN":
            st.success("🏆 승리!")
        elif result == "LOSE":
            st.error("💀 패배...")
        elif result == "DRAW":
            st.warning("🤝 무승부")
        else:
            st.info("결과 처리 중...")

    elif event_type == "TIMEOUT":
        st.error("⏰ 시간 초과 — 자동 패배")

    elif event_type == "ERROR":
        code = payload.get("code", "")
        msg = payload.get("message", "오류가 발생했습니다.")
        if code == "OPPONENT_DISCONNECTED":
            st.success("🏆 상대방 연결 끊김 — 부전승!")
        elif code == "AI_CALL_FAILED":
            st.warning(f"⚠️ AI 호출 실패 — 라운드 무효\n{msg}")
        else:
            st.error(msg)

    else:
        st.info("결과를 불러오는 중...")


def _show_comparison(payload: dict) -> None:
    my = payload.get("my_data", {})
    opp = payload.get("opponent_data") or {}

    st.subheader("📊 결과 비교")
    col1, col2 = st.columns(2)

    with col1:
        st.markdown("**나**")
        st.text_area("내 프롬프트", value=my.get("prompt", ""), height=150, disabled=True, key="my_prompt")
        st.text_area("AI 응답", value=my.get("ai_response", ""), height=100, disabled=True, key="my_response")
        st.metric("정답", f"{my.get('correct_count', 0)} / {my.get('total_count', 0)}")
        st.metric("점수", f"{my.get('score', 0):.4f}")

    with col2:
        st.markdown("**상대**")
        st.text_area("상대 프롬프트", value=opp.get("prompt", ""), height=150, disabled=True, key="opp_prompt")
        st.text_area("AI 응답", value=opp.get("ai_response", ""), height=100, disabled=True, key="opp_response")
        st.metric("정답", f"{opp.get('correct_count', 0)} / {opp.get('total_count', 0)}")
        st.metric("점수", f"{opp.get('score', 0):.4f}")


def _go_home() -> None:
    ws = st.session_state.get(s.WS_OBJECT)
    if ws:
        import ws_client
        ws_client.close(ws)
    s.reset_game_state()
    st.session_state[s.SCREEN] = "home"
    st.rerun()
```

- [ ] **Step 2: 커밋**

```bash
git add frontend/screens/result.py
git commit -m "feat: 결과 화면 (승패 + 프롬프트 비교) 구현"
```

**[사용자 테스트 가이드 - Task 6]**
1. 완전한 게임 흐름 실행 (방 생성 → 매칭 → 프롬프트 제출 × 2)
2. 결과 화면 진입 확인
3. WIN/LOSE 배너 표시 확인
4. 내/상대 프롬프트, AI 응답, 정답수, 점수 비교 표시 확인
5. "다시 하기" → 홈으로 이동 확인
6. (선택) `ARENA_AI_BACKEND=mock`으로 백엔드 실행해 빠른 채점 테스트

---

## Task 7: 에러/엣지케이스 처리

**Files:**
- Modify: `frontend/screens/round.py`
- Modify: `frontend/screens/waiting.py`
- Modify: `frontend/app.py`

- [ ] **Step 1: WS 연결 끊김 감지 — ws_client.py 수정**

`ws_client.py`의 `start_recv_thread` 수정:
```python
def start_recv_thread(ws: websocket.WebSocket, q: queue.Queue) -> threading.Thread:
    def _loop() -> None:
        while True:
            try:
                raw = ws.recv()
                if raw:
                    q.put(json.loads(raw))
            except Exception:
                # 연결 끊김 — 센티넬 이벤트 전송
                q.put({"event": "WS_CLOSED"})
                break

    t = threading.Thread(target=_loop, daemon=True)
    t.start()
    return t
```

- [ ] **Step 2: round.py의 _poll_events에 WS_CLOSED 처리 추가**

`round.py`의 `_poll_events()` 내부:
```python
        elif ev == "WS_CLOSED":
            st.session_state[s.RESULT_PAYLOAD] = {
                "event": "ERROR",
                "code": "WS_CLOSED",
                "message": "서버 연결이 끊어졌습니다.",
            }
            st.session_state[s.SCREEN] = "result"
            changed = True
```

- [ ] **Step 3: waiting.py의 _poll_events에 WS_CLOSED 처리 추가**

`waiting.py`의 `_poll_events()` 내부:
```python
        elif ev == "WS_CLOSED":
            st.error("서버 연결이 끊어졌습니다.")
            st.session_state[s.SCREEN] = "home"
            s.reset_game_state()
            changed = True
```

- [ ] **Step 4: result.py의 _show_banner에 WS_CLOSED 처리 추가**

`result.py`의 `_show_banner()` 내부 ERROR 블록:
```python
        elif code == "WS_CLOSED":
            st.error("서버 연결이 끊어졌습니다. 다시 시도해주세요.")
```

- [ ] **Step 5: 커밋**

```bash
git add frontend/ws_client.py frontend/screens/round.py frontend/screens/waiting.py frontend/screens/result.py
git commit -m "feat: WS 연결 끊김 + 엣지케이스 처리"
```

**[사용자 테스트 가이드 - Task 7]**
1. 대기 화면에서 백엔드 서버 강제 종료 → 홈으로 이동 + 에러 메시지 확인
2. 라운드 진행 중 백엔드 강제 종료 → 결과 화면에서 "연결 끊김" 표시 확인
3. 1200자 초과 입력 시도 → `max_chars=1200` 제한으로 차단 확인
4. 전체 흐름 2회 반복 (다시 하기 → 새 게임) 정상 동작 확인
